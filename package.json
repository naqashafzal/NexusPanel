{
  "name": "hostsphere-panel",
  "version": "1.0.0",
  "private": true,
  "description": "HostSphere Panel - Modern Hosting Panel",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r --parallel run start",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "@types/multer": "^2.1.0",
    "typescript": "^5.0.0"
  }
}
